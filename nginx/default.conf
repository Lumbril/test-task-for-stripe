upstream Awooo {
    server web:8000;
}

server {
    listen 80;
    server_name ${NGINX_DOMAIN} www.${NGINX_DOMAIN};

    location / {
        proxy_pass http://Awooo;
    }

    location /static/ {
      root /var/www;
    }

    location /media/ {
      root /var/www;
    }
}